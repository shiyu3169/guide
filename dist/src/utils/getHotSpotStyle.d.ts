import { SinglePlacement, Placement } from '../typings/guide';
export declare const getHotSpotStyle: (placement: Placement | undefined, arrowStyle: Record<SinglePlacement, number>) => Record<string, number>;
